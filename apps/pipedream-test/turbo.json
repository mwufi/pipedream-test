{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["PIPEDREAM_ENVIRONMENT", "PIPEDREAM_CLIENT_ID", "PIPEDREAM_CLIENT_SECRET", "PIPEDREAM_PROJECT_ID"],
      "outputs": [
        "build/**",
        ".vercel/**",
        "dist/**",
        ".next/**",
        "!.next/cache/**"
      ]
    }
  }
}
